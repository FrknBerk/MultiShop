@model List<ResultCategoryDto>
@*
    For more information on enabling MVC for empty projects, visit http://go.microsoft.com/fwlink/?LinkID=397860
*@
<div class="container-fluid pt-5">
    <h2 class="section-title position-relative text-uppercase mx-xl-5 mb-4"><span class="bg-secondary pr-3">Öne Çıkan Kategoriler</span></h2>
    <div class="row px-xl-5 pb-3">
        @if (Model != null)
        {
            @foreach (var item in Model)
            {
                <div class="col-lg-3 col-md-4 col-sm-6 pb-1">
                    <a class="text-decoration-none" href="">
                        <div class="cat-item d-flex align-items-center mb-4">
                            <div class="overflow-hidden">
                                <img class="img-fluid" src="@item.ImageUrl" alt="" style="width: 100px; height: 100px;">
                            </div>
                            <div class="flex-fill pl-3">
                                <h6>@item.CategoryName</h6>
                                <small class="text-body">0 Ürün</small>
                            </div>
                        </div>
                    </a>
                </div>
            }
        }
    </div>
</div>